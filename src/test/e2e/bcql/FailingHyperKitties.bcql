// Author: Paul Beck
SET EXCEPTION HANDLING PARAMS true "./FailingHyperKitties"

SET BLOCKCHAIN "Hyperledger"

SET CONNECTION {
   "hyperledger/connection-org1.yaml",
   "hyperledger/server.key",
   "hyperledger/server.crt",
   "Org1MSP",
   "mychannel"
}

SET OUTPUT FOLDER "./FailingHyperKitties"

BLOCKS (32) (91) {
   LOG ENTRIES ("kitties") (Birth(string a)) {
   }
}
